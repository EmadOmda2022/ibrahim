name: Deploy to Oracle Cloud

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: 89.168.127.221
          username: ubuntu
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            # التعديل النهائي للمسار الصحيح
            cd /home/shorabalawfirm/htdocs/shorabalawfirm.com
            git pull origin main
            php artisan config:cache
